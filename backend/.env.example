# 產生方式: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
WEBHOOK_BASE_URL=https://your-tunnel-domain.com
DB_PATH=data/data.db
ADMIN_PASSWORD=
DEALER_CONTACT_INFO=如需購車相關服務，請聯繫業務窗口：(02)1234-5678
# 系統異常（NotebookLM 查詢失敗、tunnel 掛掉又恢復等）時，主動推送
# LINE 訊息通知管理員。ADMIN_LINE_USER_ID 是要接收告警的人的 LINE
# userId，可用逗號分隔設定多個人（例如 U111,U222,U333），不是只能
# 一個人收到。ADMIN_ALERT_ACCESS_TOKEN 是任一個已綁定頻道的 channel
# access token（每個收件人都必須是該官方帳號的好友才收得到 push
# 訊息）。留空則不啟用告警。
ADMIN_LINE_USER_ID=
ADMIN_ALERT_ACCESS_TOKEN=