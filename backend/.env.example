# 產生方式: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=
WEBHOOK_BASE_URL=https://your-tunnel-domain.com
DB_PATH=data/data.db
ADMIN_PASSWORD=
DEALER_CONTACT_INFO=不過我可以幫您查詢車輛的規格、配備、現有庫存、年份與顏色等資訊，歡迎直接告訴我您想了解的車款！
# 系統異常（NotebookLM 查詢失敗、tunnel 掛掉又恢復等）時，主動推送
# LINE 訊息通知管理員。ADMIN_LINE_USER_ID 是要接收告警的人的 LINE
# userId，可用逗號分隔設定多個人（例如 U111,U222,U333），不是只能
# 一個人收到。ADMIN_ALERT_ACCESS_TOKEN 是任一個已綁定頻道的 channel
# access token（每個收件人都必須是該官方帳號的好友才收得到 push
# 訊息）。留空則不啟用告警。
ADMIN_LINE_USER_ID=
ADMIN_ALERT_ACCESS_TOKEN=
# 同一則告警也可以（或改為只）送到 Google Chat，走 Incoming Webhook，
# 完全不佔用 LINE 的每月訊息額度。在 Google Chat 空間設定
# 「應用程式和整合」→「新增 Webhook」取得網址填在這裡；留空則不啟用。
# 注意：這個「新增 Webhook」功能目前只開放給 Google Workspace 帳號，
# 個人 Gmail 帳號建立的空間通常會顯示「Webhook 管理功能受到限制」，
# 這種情況請改用下面的 Telegram。
GOOGLE_CHAT_WEBHOOK_URL=
# 同一則告警也可以（或改為只）送到 Telegram，走 Bot API，一樣完全
# 不佔用 LINE 的每月訊息額度，且個人帳號即可申請、不像 Google Chat
# 的 Webhook 管理功能只開放給 Workspace 帳號。設定方式：
#   1. 跟 @BotFather 對話，用 /newbot 建立一個 bot，取得 TELEGRAM_BOT_TOKEN
#   2. 跟這個 bot 隨便說一句話，再打開
#      https://api.telegram.org/bot<TOKEN>/getUpdates
#      從回應的 JSON 裡找到 "chat":{"id": ...}，就是 TELEGRAM_CHAT_ID
# 兩者都留空則不啟用。
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=